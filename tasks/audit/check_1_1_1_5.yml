---

- name: Setup fact with check description
  set_fact:
    cis_check_description: Ensure mounting of hfsplus filesystems is disabled

- name: "{{ cis_check_description }}"
  vars:
    check_filesystem_type: hfsplus
  include_tasks: ../common/check_kernel_module.yml
  no_log: "{{ cis_no_log }}"
