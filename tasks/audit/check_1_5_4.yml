---

- name: Test to ensure prelink is disabled
  yum:
    name: prelink
    state: absent
  register: prelink_check_result
  check_mode: yes

- name: Ensure prelink is disabled
  assert:
    that:
      - prelink_check_result is not changed
  register: cis_check_result
